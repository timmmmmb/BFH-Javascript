/**
 * constructor for a Char
 * @param {*} value the char value
 * @param {*} string the chars representation as a string
 */
function Char(value, string){
    this.string = string;
    this.value = value;
}